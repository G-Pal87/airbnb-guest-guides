# Git workflow

After completing any change (content, data, components, config, dependencies — everything), push it to the working branch, open a PR into `main`, and merge that PR immediately, without waiting for confirmation.

Only skip the merge — leaving the change on its branch — if the user explicitly says to hold off, or asks for review first.
